menu hshift 0
menu width 82
# The Sysible logo + wordmark occupy the TOP band of splash.png (up to ~y=240).
# Shift the whole menu (title + entries) down into the clear LOWER band so the
# boot text never overlaps the artwork.
menu vshift 16
menu rows 8
menu titlerow 14
menu tabmsgrow 26
menu cmdlinerow 26
menu helpmsgrow 28
menu helpmsgendrow 29

menu title Sysible Linux
include stdmenu.cfg
include live.cfg
include install.cfg
menu end
